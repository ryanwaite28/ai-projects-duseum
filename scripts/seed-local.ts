#!/usr/bin/env tsx
/**
 * scripts/seed-local.ts
 *
 * Seeds MiniStack (localhost:4566) with test data for local development.
 * Requires MiniStack to be running: docker-compose up -d
 *
 * Usage:
 *   npx tsx scripts/seed-local.ts
 *
 * TODO: PROMPT-1.6 — implement seed data for Users, Authors, ArtPieces, Subscriptions
 */

console.log('seed-local: TODO — implement in PROMPT-1.6')
